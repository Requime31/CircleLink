{
  "name": "circlelink-websocket-server",
  "version": "1.0.0",
  "description": "CircleLink MVP WebSocket server — auth, room routing, message broadcast",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "cleanup:accounts": "node src/cleanup/cli.js",
    "cleanup:accounts:dry-run": "node src/cleanup/cli.js --dry-run",
    "cleanup:accounts:mock-dry-run": "node scripts/mockDryRun.js",
    "test": "node --test",
    "check": "node --check src/cleanup/accountCleanup.js && node --check src/cleanup/firebaseCleanupAdapter.js && node --check src/cleanup/supabaseStorageAdapter.js && node --check src/cleanup/cli.js && node --check scripts/migratePrivateAccountData.js",
    "migrate:private-account": "node scripts/migratePrivateAccountData.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "uuid": "^11.0.0",
    "ws": "^8.18.0"
  }
}
