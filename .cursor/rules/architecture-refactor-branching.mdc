---
description: Architecture refactor branching — one step branch, squash into parent, rename after each phase
alwaysApply: true
---

# CircleLink — Architecture Refactor Branching

Use this workflow until architecture phases **1–33** are finished.

## Branches

| Role | Name |
|------|------|
| Stable | `main` — do not change for this work |
| Integration | `develop` |
| Parent (accumulator) | `feature/architecture-refactoring` (from `develop`) |
| Working (one at a time) | `step-N` → rename to `step-(N+1)` after each phase |

There is **only one** working step branch at a time. Do not create `step-01`…`step-33` in parallel.

## Cycle (every phase)

1. Implement **only** the current phase on `step-N` (prompt in `docs/architecture-phase-prompts/`).
2. Verify build for the touched area.
3. Commit on `step-N` when the user asks (message: `Phase N: <short description>`).
4. **Squash-merge** into parent:
   ```bash
   git checkout feature/architecture-refactoring
   git merge --squash step-N
   git commit -m "Phase N: <short description>"
   ```
5. Rename working branch and align to parent tip (required after squash):
   ```bash
   git branch -m step-N step-(N+1)
   git checkout step-(N+1)
   git reset --hard feature/architecture-refactoring
   ```
6. Update remotes:
   ```bash
   git push -u origin feature/architecture-refactoring
   git push -u origin step-(N+1)
   git push origin --delete step-N
   ```
7. Continue the next phase on `step-(N+1)`.

## PR policy

- **No PR per phase.**
- **One PR only after phase 33:** `feature/architecture-refactoring` → `develop`.
- Do not open PRs into `main` for this refactor.

## Hard rules

- Do not mix phases in one step commit / squash.
- Do not push architecture work to `main`.
- After every squash, **reset** the renamed step branch to the parent tip before new work (avoids duplicate diffs on the next squash).
- Parent is created from `develop`; final merge target is `develop` when all phases are stable.

## Repo

- Remote: `https://github.com/Requime31/CircleLink.git`
- Prompts: `docs/architecture-phase-prompts/`
